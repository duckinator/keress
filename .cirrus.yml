test_task:
  container:
    image: duckinator/godot-builder:latest
  install_script:
    - make ci-setup
  script:
    - make test

nightly_task:
  only_if: $BRANCH == 'nightly'
  #depends_on:
  #  - test
  container:
    image: duckinator/godot-builder:latest
  install_script:
    - make ci-setup
  script:
    - make ci-nightly
